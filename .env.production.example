# Production (ECS / RDS). Do not commit real values — set these in the task definition
# or secrets manager. Copy only as a checklist of required vars.

DATABASE_URL=postgres://USER:PASSWORD@ORCHEX_RDS_HOST:5432/orchex?sslmode=require
HTTP_ADDR=:8080
